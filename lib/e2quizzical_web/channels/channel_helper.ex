defmodule E2QuizzicalWeb.ChannelHelper do
  alias E2QuizzicalWeb.{Endpoint}

  # def send_update(channel_id_string, dock, user_id) do
  #   Endpoint.broadcast(channel_id_string, "update_message", %{
  #       "message" => "#{dock.dock_name} has been updated.",
  #       "dock" => dock,
  #       "user_id" => user_id
  #   })
  # end
end
